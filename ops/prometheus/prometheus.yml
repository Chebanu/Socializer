global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Uncomment once Socializer.Api exposes a /metrics endpoint (Sprint 11 - Observability).
  # - job_name: socializer-api
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets: ["api:8080"]
